{
  "name": "dhcar",
  "version": "1.0.0",
  "description": "Digitizing Health Centre Appointments and Records",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "cd client && ng serve",
    "build": "cd client && ng build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express-validator": "^7.0.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "keywords": [
    "healthcare",
    "appointments",
    "medical-records",
    "mean-stack"
  ],
  "author": "DHCAR Team",
  "license": "MIT"
}
